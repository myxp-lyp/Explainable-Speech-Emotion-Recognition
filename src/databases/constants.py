labels = ["A", "D", "F", "H", "N", "S"]
label2id = {label: i for i, label in enumerate(labels)}
id2label = {i: label for i, label in enumerate(labels)}

demographic_columns = ["AgeGroup", "Ethnicity", "Sex", "Race"]
